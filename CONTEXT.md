# MiniAgent

一个 Agent harness 工程：把「循环」与「策略」分开，并让会话历史以可重放的日志形态存在。本文件是本项目的词汇表，只定义概念本身，不含实现细节。

## Language

### 会话与日志

**会话（Session）**：
一次对话的完整历史。它本身是事件日志，不是一个消息数组。
_Avoid_: 对话记录、history、上下文

**事件（Event）**：
日志里的一条带单调递增序号、带类型标签的记录。一件事发生过就追加一条，永不修改。
_Avoid_: 日志行、记录项

**事件日志（Session Log）**：
按发生顺序追加的会话事件序列，是本工程唯一的权威源。历史可以重放，也可以审计。
_Avoid_: 消息列表、transcript、历史文件

**权威源（Source of Truth）**：
唯一可以据以重建其它一切的存储。在本工程中它就是事件日志，别的东西都是它算出来的。
_Avoid_: 主存储、数据库、真相

**投影（Projection）**：
把事件日志映射为「模型可见历史」的那个纯函数。它读日志、不改日志，输出不是执行过程的记录，只是模型将要看到的内容。
_Avoid_: 上下文、prompt、对话历史、模型状态

**模型可见历史（Model-visible History）**：
投影的输出，也就是下一次模型调用实际收到的那些消息。凡是进入这里的内容，都必须能从事件日志重建。
_Avoid_: messages、prompt、上下文窗口

**派生数据（Derived Data）**：
任何由权威源算出、因此可以随时丢弃并重建的数据——投影、索引、缓存都属于此类。派生数据损坏时重建，不迁移。
_Avoid_: 缓存文件、副本、快照

### 循环与策略

**回合（Turn）**：
从一次用户输入，到一个最终答复之间的完整过程。一个回合内可能发生多次模型采样。
_Avoid_: 轮次、请求、session

**步骤（Step）**：
回合内的一次「模型采样 → 执行它请求的工具」。回合由一个或多个步骤组成，受上限约束。
_Avoid_: 迭代、轮、cycle

**策略（Policy）**：
对循环行为的干预，例如重试、超时、压缩、输出校验、终结判定。策略不写在循环里。
_Avoid_: hook、中间件、插件、逻辑

**策略插件（Policy Plugin）**：
以「订阅事件」的形式接入的策略。它可以观察、改写入参、短路或收尾，但循环本身不认识它。
_Avoid_: 扩展、模块、handler

**终结工具（Terminal Tool）**：
一种执行成功即以自己的内容结束本回合、不再继续采样的工具。它由策略声明，循环不认识具体工具名。
_Avoid_: 输出工具、结束工具、final

### 工具执行

**工具（Tool）**：
模型可以请求执行的一项能力。工具只声明「做什么」，不负责进程生命周期。
_Avoid_: 函数、action、命令

**技能（Skill）**：
一组工具加一段领域提示，按需装载与卸载；装载则其工具可用，卸载即撤销。
_Avoid_: 模块、插件、能力包

**中止结局（Abort Outcome）**：
工具未正常完成时的分类结局——超时、被取消、被策略拒绝、执行失败。四者必须可区分，因为它们导向不同的重试与呈现。
_Avoid_: 错误、失败、异常

**受管范围（Managed Range）**：
一次工具执行所拥有的整棵进程树（含后代）。终止以它为单位，而不是以单个进程为单位，这样才不会留下孤儿。
_Avoid_: 子进程、进程池、线程
