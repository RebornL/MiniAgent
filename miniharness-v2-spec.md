# miniharness v2 规格：事件流持久化、进程级终止、按能力族包化

> 状态：待实现。来源：`Codex-Harness-Persistence-and-Recovery.md`、`DeepSeek-Harness-Persistence-and-Recovery.md`（源码级调研，引用均已核验）+ `docs/review-guide-v1.md` 标记的已知张力。发布标签：`ready-for-agent`。

## Problem Statement

miniharness 第一版把「循环」和「策略」分开了，但三处根基仍依赖旁路或不可靠的承诺：

1. **持久化的权威源是投影，不是日志。** 落盘的是 `derive_messages()` 的结果加一份元数据旁路（摘要文本、已激活技能名）。压缩事件与技能装载事件**不落盘**，恢复靠「消息拼装 + 旁路元数据」而不是重放。一旦旁路与日志不一致（改名、部分写、格式演进），历史无法可靠重建——这直接违背项目自己的核心不变量「凡进模型的内容都必须能从会话日志重建」。
2. **超时只承诺「停止等待」。** 被放弃的工具体仍在后台线程里跑到底，可能留下半成品文件；`deny`/超时/取消三种中断在结果里不可区分（都塌成 `status=error`），下游无法做不同的重试与呈现策略。两个生产级 harness 的答案一致且与直觉相反：**它们都不做副作用回滚**，而是把终止能力下沉到进程边界（沙箱只包装 argv，终止归 subprocess，进程组 TERM→KILL）。
3. **工程没有落位规则。** 9 个平铺模块加一个文档目录，能力边界靠文件名暗示，新增能力时没有「放哪、怎么命名、按什么拆」的成文规则。两个上游给出的共识不是目录名，而是三条规则：按能力族分组、命名按「当前存在的角色」、按变化速率拆包。

## Solution

1. **持久化改为「权威事件流 + 可丢弃的派生索引」**：会话历史以事件日志落盘，模型可见消息永远是派生视图；压缩与技能装载成为一等事件，可重放；写盘走有界写后缓冲加显式 flush 屏障。
2. **工具执行下沉到进程边界**：沙箱 seam 只包装 argv（不可用即 fail-closed），终止归独立的 subprocess seam（managed range + TERM→KILL）；超时与取消规范化为结构化结果并携带稳定错误码，区分「超时」「取消」「被拒」「失败」。
3. **按能力族包化**：能力落成 Definition / Provider / Consumer 三个具名角色，包按变化速率拆分，新代码一律按此落位。

三者互相咬合：① 让「恢复」变成重放，② 让「超时」不再是谎话，③ 让前两者的边界在目录上看得见。

## User Stories

1. 作为一个 Agent 使用者，我想在进程崩溃后重启并继续上次会话，以便不丢失任何已完成的工作。
2. 作为一个 Agent 使用者，我想恢复后的会话内容与崩溃前逐字一致，以便我信任恢复结果。
3. 作为一个 Agent 使用者，我想恢复是「重放日志」而不是「信任一份可能过期的派生快照」，以便历史永远自洽。
4. 作为一个调试者，我想读出某次会话的完整事件流，以便事后复盘模型与工具到底发生了什么。
5. 作为一个调试者，我想区分「历史里发生过什么」与「模型最终看到了什么」，以便定位是记录错了还是投影错了。
6. 作为一个会话消费者，我想在崩溃恢复时保留完整的尾轮，只在尾部缺失处补上闭合事件，以便不丢已落盘的完整数据。
7. 作为一个会话消费者，我想知道某次会话的日志格式版本，以便不同版本的记录能被正确翻译而不是被误读。
8. 作为一个策略作者，我想让压缩结果本身成为一条可重放记录（含被遮蔽范围与替换内容），以便压缩后的历史能被确定性重建。
9. 作为一个策略作者，我想让技能装载/卸载成为日志里的一等事件，以便恢复时无需依赖旁路元数据。
10. 作为一个会话消费者，我想把派生视图缓存为「可丢弃」的，以便索引损坏时直接重建而不影响权威数据。
11. 作为一个 Agent 使用者，我想在工具超时后确信那个工具体真的不再运行，以便它不会在我不知情时留下半成品。
12. 作为一个 Agent 使用者，我想在超时/取消后进程树被真正清理（含后代进程），以便不产生孤儿进程。
13. 作为一个 Agent 使用者，我想区分「工具超时」「被用户取消」「被策略拒绝」「执行失败」四种结局，以便对不同结局采取不同策略。
14. 作为一个策略作者，我想重试策略能按稳定错误码决定是否重试，以便不重试那些重试也无用的结局。
15. 作为一个安全维护者，我想在沙箱不可用时执行直接失败而不是静默降级，以便隔离承诺不被悄悄打破。
16. 作为一个工具作者，我想工具只需声明 argv 或调用意图，不必自己处理进程终止，以便终止语义只有一处实现。
17. 作为一个开发者，我想新增一个能力时能一眼看出它属于哪个能力族、该放在哪一层，以便目录结构自我解释。
18. 作为一个开发者，我想包按「变化速率」拆分，以便频繁变动的策略不会牵连稳定的契约。
19. 作为一个开发者，我想命名反映「当前存在的角色」而不是预留的将来，以便不出现空壳包。
20. 作为一个维护者，我想在重构后既有行为逐字不变，以便迁移不破坏已工作的功能。
21. 作为一个既有用户，我想第一版的三个测试 seam 继续有效，以便重构有安全网。
22. 作为一个维护者，我想新增的能力边界都有成文规则可依，以便后续不再靠口口相传。

## Implementation Decisions

### 持久化：事件流为权威，投影为派生

- **事件日志是唯一权威源**。模型可见消息、压缩摘要、技能状态、追踪记录全部由日志派生；任何「并行存储的模型可见历史」都必须移除。
- **物理形态**用「逐行一条事件 + 首行 header」的追加式文本记录，可选压缩；header 携带格式版本与创建元数据。日志文件按格式代际命名，写入只发布当代版本。
- **事件词汇表**至少包含：回合开始/结束、用户消息、助手消息（含工具调用）、工具结果、工具被拒、压缩开始/摘要/结束、技能装载/卸载、系统提示更新。事件带单调递增序号。
- **压缩是可重放事件**：记录「被遮蔽的事件区间」与「替换内容」，被遮蔽事件仍留在日志里，仅在投影时遮蔽——因此压缩后的历史可确定性重建，且支持「反向找最后一个检查点、正向重放补齐」的恢复算法。
- **模型可见内容的唯一例外**是「为塞进上下文窗口而在内存中做的裁剪」，此裁剪不回写日志（上游两方均如此）；该例外必须显式标注，不得成为隐藏行为。
- **写盘**：事件先入内存日志，再经**有界写后缓冲**批量落盘；对外暴露显式 `flush` 屏障，只有屏障返回才构成崩溃承诺。写失败时把未落盘项留在队列里以便重试，不丢半条记录。
- **检查点策略**在三个语义点做 fail-closed 落盘：向模型发起请求前、顶层工具派发前、每步开始前。
- **恢复算法**：先取独占写所有权，再读全量日志，合成尾部缺失的闭合事件并写回，最后以「已落盘事件 + 补齐事件」为种子重建。**绝不截断或改写已落盘的完整事件**。
- **派生的索引/缓存可丢弃**：以「键 + 版本 + 序号」标识，版本不匹配直接丢弃重建，不做迁移。
- **格式演进**用相邻版本迁移链，写入只发布当代版本。

### 工具中止：语义化结果 + 进程级终止

- **取消与超时规范化为结构化结果值**，与成功同构（不靠抛出裸异常表达）。结果携带稳定错误码，至少区分：`timed_out`、`cancelled`、`denied`、`failed`。
- **已启动的工具体必须跑到静止**（quiescence）后才允许替换其结果；未启动的调用不启动并直接给出结构化结果。
- **调用与结果必须成对**：模型消息里出现的每个工具调用，都必须在同一批内得到一条结果记录。取消时为未启动的调用补合成结果；恢复时对未闭合的调用再补一次。
- **沙箱 seam 只负责 argv 包装**：接收调用意图与策略，返回可执行的 argv 与完整性要求；不可用时 fail-closed 报错，**禁止静默透传无约束执行**。沙箱不负责终止。
- **终止归独立的 subprocess seam**：以「受管范围（managed range）」为本位——启动组、等待组内退出、终止整组（含后代）。POSIX 用信号组升级（先 TERM、宽限期后 KILL），Windows 用进程树终止。终止接口幂等；释放时等待受管范围退出以防孤儿。
- **流程**：超时或取消 → 请求终止受管范围 → 等宽限期 → 升级为强杀 → 收集已产生的输出 → 以结构化结果返回。
- **超时的默认值与退出码语义**保留既有约定（默认超时沿用现有常量；超时给出可识别的退出码/错误码），以便下游区分。

### 包化：按能力族与变化速率

- **能力 = 三个具名角色**：Definition（契约 + 类型 + 事件词汇表）、Provider（实现）、Consumer（工具/策略）。契约与实现分属不同包，消费方只依赖契约。
- **按能力族分组**：每个包恰属一个族；每族有一份权威的包地图（README）说明该族的包与职责；新包优先并入已有族。
- **按变化速率拆包**：契约（低频）与策略/后端（高频）分离，使频繁变动不牵连稳定接口。
- **命名按「当前存在的角色」**：命名描述它现在是什么，而不是将来可能变成什么；不创建空壳包。
- **测试与实现同层但分离**：包内测试放独立文件（不以测试代码污染实现文件）；跨包集成测试集中一处。
- **根级工程规范成文**：新代码的落位、命名、依赖方向、测试放置写成一份权威规范，供人与 agent 共同遵循。
- **迁移不改行为**：包化是纯搬移 + 导入调整，行为语义逐字不变；既有测试是安全网。

## Testing Decisions

- **好测试只测外部可观察行为**：断言「重启后会话内容与崩溃前一致」「超时后目标进程真的消失」「投影可由日志重建」，不断言内部字段名、文件布局或中间状态。
- **已确认的 seam 共四个**：复用第一版的三个（S2/S3/S5），加上本规格唯一的新 seam（进程边界）。不新增其它 seam。
  - **S2（turn 边界，集成）**：驱动一次 turn，断言落盘内容、恢复往返、中止结局。
  - **S3（工具执行管线，契约）**：断言四种中止结局的区分与调用-结果成对。
  - **S5（会话投影，纯函数）**：断言投影确定幂等、压缩可重放、日志往返等价。
- **新增的唯一新 seam：进程边界**（真实子进程）。这是 ① 唯一无法在进程内验证的部分——「进程真的没了」必须由操作系统证明。测试需断言：超时后进程组不再存在、无孤儿后代、终止幂等。
- **测试数量保持克制**：每个 seam 少量高价值用例，不为覆盖率堆测试。
- **Prior art**：第一版的 25 条测试即先例；上游两方也各有 `test-support`/`*_tests.rs` 与集成套件可参照其断言层次。

## Out of Scope

- 远端执行服务与跨进程租约/并发保护。
- 多 provider 路由与凭据管理。
- fork / revert（不可变新文件 + 指针 CAS）——本阶段只要求日志形态**可支撑**它，不要求实现。
- UI / SDK / 协议解耦。
- 静态加密、事件行大小上限等 at-rest 策略。
- 压缩算法本身的改进（只要求压缩结果可持久化与可重放）。
- 删除第一版的兼容路径——迁移采取并存过渡，行为不变优先。

## Further Notes

- 调研依据（引用均已逐条核验）：`Codex-Harness-Persistence-and-Recovery.md`、`DeepSeek-Harness-Persistence-and-Recovery.md`。
- 两方的关键共识（与直觉相反，故显式记录）：**都不做副作用回滚**；正确形态是「隔离 + 拒绝检测 + 进程级终止」。**都不把消息历史当权威存储**；消息永远是派生视图。
- 本规格的硬约束：任何「并行存储模型可见历史」的实现都视为偏离；中止结局必须可区分；沙箱不可用必须 fail-closed。
